#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin>>n;

    int sum=0;

    for(int i=0;i<n;i++){

        long long x;
        cin>>x;
        sum+=x;
    }

    cout<<abs(sum)<<endl;

    return 0;
}

